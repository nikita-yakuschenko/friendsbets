/** Интервал фонового опроса live-матчей на Championat. */
export const CHAMPIONAT_LIVE_WORKER_POLL_MS = 60_000;

/** Если синк старше — API помечает данные как stale (UI показывает предупреждение). */
export const CHAMPIONAT_LIVE_DATA_STALE_MS = 90_000;
