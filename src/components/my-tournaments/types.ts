export type MyTournamentRow = {
  id: string;
  title: string;
  inviteCode: string;
  inviteLinkUrl: string;
  createdAt: string;
  scoringRuleTitle: string;
  participantsCount: number;
  canLeave: boolean;
};
