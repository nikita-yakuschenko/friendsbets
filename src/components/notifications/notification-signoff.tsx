import { NOTIFICATION_SIGNOFF } from "@/lib/notification-signoff";

export function NotificationSignoff() {
  return (
    <p className="pt-2 text-xs text-brand-muted">{NOTIFICATION_SIGNOFF}</p>
  );
}
